name: Reusable Deploy Workflow

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      image_tag:
        required: true
        type: string
      docker_image_name:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.12.0

      - name: Create k8s Kind Cluster
        uses: helm/kind-action@v1
        with:
          config: ./cluster.yml

      - name: Helm Install (Dry Run)
        run: |
          helm install --dry-run --debug todo-release ./helm-charts/todoapp \
            --set image.repository=${{ inputs.docker_image_name }} \
            --set image.tag=${{ inputs.image_tag }}

      - name: Helm Upgrade --install (Atomic)
        run: |
          helm upgrade --install todo-release ./helm-charts/todoapp \
            --atomic \
            --wait \
            --timeout 5m \
            --set image.repository=${{ inputs.docker_image_name }} \
            --set image.tag=${{ inputs.image_tag }}